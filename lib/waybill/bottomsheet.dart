import 'package:flutter/material.dart';

class WaybillBottomsheet extends StatelessWidget {
  const WaybillBottomsheet({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}