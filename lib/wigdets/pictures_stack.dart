import 'package:flutter/material.dart';

class PictureStack extends StatelessWidget {
  const PictureStack({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
